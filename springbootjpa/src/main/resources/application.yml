spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jpatest?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456

  jpa:
    generate-ddl: true    # 如果数据库中无表自动创建该表
    show-sql: true